{
  "name": "deepstream.io-storage-dynamodb-internal",
  "version": "1.0.0",
  "description": "AWS DynamoDB connector for deepstreamHub",
  "main": "src/connector.js",
  "scripts": {
    "test": "mocha",
    "coverage": "istanbul cover _mocha",
    "lint": "eslint \"src/**/*.js\" \"test/**/*.js\"",
    "ci": "npm run coverage && npm run lint"
  },
  "homepage": "http://deepstream.io",
  "dependencies": {
    "aws-sdk": "^2.6.9"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.9",
    "eslint": "^2.11.1",
    "eslint-config-airbnb": "^3.1.0",
    "istanbul": "^0.4.3",
    "mocha": "^2.5.2"
  }
}
